# ai-dev-kit local install artifacts
.ai-dev-kit/
.agents/
# agent orchestration state (registry, worktrees, review notes) -- local only, never
# part of a feature branch. Unanchored so it matches .polly/ at any depth.
.polly/
.worktrees/
# python
__pycache__/
*.pyc
.venv/
venv/
.env
# node
node_modules/
dist/
build/
# databricks
.databricks/
